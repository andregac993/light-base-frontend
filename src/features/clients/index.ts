export { ClientCard, ClientForm, ClientList } from './components';

export { useClientActions, useClientById, useClientSearch } from './hooks';

export { clientSchema } from './schemas';
export type { ClientFormData } from './schemas';

export type { Client, CreateClientDTO, UpdateClientDTO, ClientFilters } from './types';
